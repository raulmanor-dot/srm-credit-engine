# Copie para .env se quiser sobrescrever os defaults do docker-compose.yml.
# Sem esse arquivo, os defaults abaixo já são usados (srm/srm).
POSTGRES_DB=srm_credit_engine
POSTGRES_USER=srm
POSTGRES_PASSWORD=srm
